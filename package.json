{
  "name": "questions-howtogetstartupideas",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "//dev": "start dev server, aliases: `vite dev`, `vite serve`",
    "dev": "vite",
    "//build": "build for production",
    "build": "vite build",
    "//preview": "locally preview production build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.13"
  },
  "dependencies": {
    "firebase": "^9.8.4"
  }
}
